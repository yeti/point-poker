/* eslint-disable */

import React from 'react';
import { expect } from 'chai';
import { mount } from 'enzyme';

describe('Home', () => {
  it('sample test', () => {
    expect(1).to.equal(1);
  });
});
